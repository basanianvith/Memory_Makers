@{
    ViewData["Title"] = "My Bookings";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

<div class="container py-5">
    <div class="row mb-4 align-items-end">
        <div class="col-md-6">
            <h2 class="fw-bold text-dark">My Bookings</h2>
            <p class="text-muted">Manage your upcoming trips and view your travel history.</p>
        </div>
        <div class="col-md-6 text-md-end">
            <div class="btn-group shadow-sm" role="group">
                <button type="button" class="btn btn-primary">All</button>
                <button type="button" class="btn btn-white border">Upcoming</button>
                <button type="button" class="btn btn-white border">Completed</button>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr class="text-muted small text-uppercase">
                            <th class="ps-4 py-3">Package</th>
                            <th class="py-3">Dates</th>
                            <th class="py-3">Guests</th>
                            <th class="py-3">Amount</th>
                            <th class="py-3">Status</th>
                            <th class="py-3 text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="ps-4">
                                <div class="d-flex align-items-center">
                                    <div class="rounded-3 bg-primary text-white d-flex align-items-center justify-content-center me-3" style="width: 50px; height: 50px;">
                                        <i class="fas fa-umbrella-beach fa-lg"></i>
                                    </div>
                                    <div>
                                        <h6 class="mb-0 fw-bold">Tropical Maldives Escape</h6>
                                        <small class="text-muted">Ref: #BK-8802</small>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <div class="small fw-semibold">Oct 12 - Oct 18, 2024</div>
                                <small class="text-muted">6 Nights</small>
                            </td>
                            <td><span class="badge rounded-pill bg-light text-dark border">2 Adults</span></td>
                            <td><div class="fw-bold text-primary">₹1,250.00</div></td>
                            <td>
                                <span class="badge bg-success-subtle text-success border border-success-subtle px-3 py-2">
                                    <i class="fas fa-check-circle me-1"></i> Confirmed
                                </span>
                            </td>
                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-secondary me-1" onclick="showDetails('Tropical Maldives Escape', '#BK-8802', 'Oct 12 - 18, 2024', 'Confirmed', '2 Adults', '₹1,250.00', 'fa-umbrella-beach')">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-primary" onclick="downloadTicket('#BK-8802')">
                                    <i class="fas fa-download"></i>
                                </button>
                            </td>
                        </tr>

                        <tr>
                            <td class="ps-4">
                                <div class="d-flex align-items-center">
                                    <div class="rounded-3 bg-info text-white d-flex align-items-center justify-content-center me-3" style="width: 50px; height: 50px;">
                                        <i class="fas fa-mountain fa-lg"></i>
                                    </div>
                                    <div>
                                        <h6 class="mb-0 fw-bold">Shillong: The Scotland of the East</h6>
                                        <small class="text-muted">Ref: #BK-7712</small>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <div class="small fw-semibold">Dec 05 - Dec 12, 2024</div>
                                <small class="text-muted">7 Nights</small>
                            </td>
                            <td><span class="badge rounded-pill bg-light text-dark border">1 Adult</span></td>
                            <td><div class="fw-bold text-primary">₹2,100.00</div></td>
                            <td>
                                <span class="badge bg-warning-subtle text-warning border border-warning-subtle px-3 py-2">
                                    <i class="fas fa-clock me-1"></i> Pending
                                </span>
                            </td>
                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-secondary me-1" onclick="showDetails('Shillong: Scotland of the East', '#BK-7712', 'Dec 05 - 12, 2024', 'Pending', '1 Adult', '₹2,100.00', 'fa-mountain')">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-danger" onclick="deleteRow(this)">
                                    <i class="fas fa-trash-can"></i>
                                </button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">Booking Details</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="text-center mb-4">
                    <i id="modalIcon" class="fas fa-umbrella-beach fa-3x text-primary mb-3"></i>
                    <h4 id="modalTitle" class="fw-bold"></h4>
                    <span id="modalRef" class="badge bg-light text-muted border"></span>
                </div>
                <hr>
                <div class="row g-3">
                    <div class="col-6">
                        <label class="small text-muted d-block">Dates</label>
                        <span id="modalDates" class="fw-semibold"></span>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Status</label>
                        <div id="modalStatus"></div>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Guests</label>
                        <span id="modalGuests"></span>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Total Paid</label>
                        <span id="modalAmount" class="fw-bold text-primary"></span>
                    </div>
                </div>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">Booking Details</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="text-center mb-4">
                    <i id="modalIcon" class="fas fa-umbrella-beach fa-3x text-primary mb-3"></i>
                    <h4 id="modalTitle" class="fw-bold"></h4>
                    <span id="modalRef" class="badge bg-light text-muted border"></span>
                </div>
                <hr>
                <div class="row g-3">
                    <div class="col-6">
                        <label class="small text-muted d-block">Dates</label>
                        <span id="modalDates" class="fw-semibold"></span>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Status</label>
                        <div id="modalStatus"></div>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Guests</label>
                        <span id="modalGuests"></span>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted d-block">Total Paid</label>
                        <span id="modalAmount" class="fw-bold text-primary"></span>
                    </div>
                </div>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<style>
    body {
        background-color: #f8f9fa;
    }

    .btn-white {
        background: #fff;
    }

        .btn-white:hover {
            background: #f1f1f1;
        }

    .bg-success-subtle {
        background-color: #e6fffa !important;
        color: #008767 !important;
    }

    .bg-warning-subtle {
        background-color: #fffaf0 !important;
        color: #9c6d19 !important;
    }

    .bg-danger-subtle {
        background-color: #fff5f5 !important;
        color: #e53e3e !important;
    }
</style>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
    function showDetails(title, ref, dates, status, guests, amount, icon) {
        document.getElementById('modalTitle').innerText = title;
        document.getElementById('modalRef').innerText = ref;
        document.getElementById('modalDates').innerText = dates;
        document.getElementById('modalGuests').innerText = guests;
        document.getElementById('modalAmount').innerText = amount;

        let statusClass = status === 'Confirmed' ? 'bg-success-subtle text-success' : 'bg-warning-subtle text-warning';
        document.getElementById('modalStatus').innerHTML = `<span class="badge ${statusClass} px-3 py-2">${status}</span>`;
        document.getElementById('modalIcon').className = `fas ${icon} fa-3x text-primary mb-3`;

        var myModal = new bootstrap.Modal(document.getElementById('detailsModal'));
        myModal.show();
    }

    function downloadTicket(ref) {
        alert("Downloading Ticket for " + ref + "...");
    }

    function deleteRow(btn) {
        if(confirm("Are you sure you want to cancel this booking?")) {
            btn.closest('tr').remove();
        }
    }
</script>